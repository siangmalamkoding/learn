name: Example

on:
  push:
    branches: [ main ]
jobs:
  Make:
    runs-on: ubuntu-latest
    name: Make ${{matrix.work}} ${{matrix.arch}}
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: down
        uses: MitchWind/ftp-deploy@master
        with:
            server: "ftp.apiserveronline.my.id"
            username: "cicd@apiserveronline.my.id"
            password: "MkA6}wCHXLdj"
            local_dir: "targets"
            server_dir: "public_html"
